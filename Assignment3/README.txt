Run the code with all input images placed in the TestImages folder which is in the folder containing  the '.cpp' file
You can also keep images in a folder named 'Noisy Images', in the directory TestImages

Use trackbar to choose images, filters and to change filter size.